<div class="row">
  <!-- Result List -->
  <div class="large-8 columns">
    <% if @taxis_results.empty? %>

    <% else %>
        <%= render partial: 'taxis/partials/list_taxis', locals: { taxis: @taxis_results } %>
    <% end %>

  </div>

  <!-- Search Form -->
  <div class="large-4 columns">
    <%= render partial: 'taxis/partials/form_after_search', locals: {form_data: @form} %>
  </div>

</div>