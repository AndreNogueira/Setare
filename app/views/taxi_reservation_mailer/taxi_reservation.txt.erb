Taxi Reservation Confirmation

Hello, <%= @user.name %>


The reservation of the taxi is scheduled for <%= @reservation.service_begin.strftime("%d/%m/%Y at %H:%M") %> at
the place of departure <%= @reservation.pick_up_location %>, <%= @reservation.city %> and the final
destination is <%= @reservation.drop_off_location %>.

<% unless @reservation.round_trip %>
The return is scheduled for the <%= @reservation.service_end.strftime("%d/%m/%Y at %H:%M") %> at the final destination mentioned above.
<% end %>

The price to be paid to the driver will be <%= number_to_currency @reservation.final_price, precision: 2, unit: '€', separator: ',', delimiter: '.', format: '%n%u' %>, which include luggage according to the taxi selected.
<% unless @reservation.special_request.nil? %>
Your special request is : <%= @reservation.special_request %>
<% end %>

For further questions contact the Setare team through the provided contact.

Thank you for choosing,
The Setare team
